#!/usr/bin/python3
def raise_exception():
    """
    Raise a TypeError exception.

    This function raises a built-in Python exception called `TypeError`. It does not provide a custom message
    for the exception, so the exception will have a default message.

    Raises:
        TypeError: This exception is raised without a custom message.
    """
    raise TypeError
